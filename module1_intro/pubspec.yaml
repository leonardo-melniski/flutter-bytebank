name: alurabytebank
description: My first Flutter project, study by alura's course. And this project is my sandbox for quick codes.

version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^0.1.2
  http: ^0.12.0+2
  json_annotation: ^3.0.0
  json_serializable: ^3.2.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^1.7.2

flutter:
  uses-material-design: true